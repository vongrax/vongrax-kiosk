import { WindowConfig } from '../interfaces/window-config.interface';

export const windowConfig: WindowConfig = {
  kiosk: true, // режим киоска
  alwaysOnTop: true, // поверх других окон
  frame: false, // создать безрамное окно
};
